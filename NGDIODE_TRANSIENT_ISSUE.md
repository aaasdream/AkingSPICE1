# NgDiode 瞬態分析問題診斷報告

## 問題描述

**NgDiode 在瞬態分析中輸出始終為 0V**，即使輸入電壓在變化。

## 測試用例

### 測試 1: 簡單 DC 偏壓（固定 5V）
- 電路：Vdd(5V) -> R(1kΩ) -> D -> GND
- 結果：✅ **成功** - V_diode = 0.693V（正常）

### 測試 2: AC 整流（無濾波電容）
- 電路：AC(5V, 1kHz) -> D -> R(1kΩ) -> GND  
- 結果：❌ **失敗** - V_out = 0.000V（應該是半波整流）

### 測試 3: AC 整流（有濾波電容）
- 電路：AC(12V, 60Hz) -> D -> R(1kΩ) || C(100μF) -> GND
- 結果：❌ **失敗** - V_out = 0.000V（應該是平滑的 DC）

## 關鍵觀察

1. **DC 分析正常**：固定電壓源時，二極體工作正常
2. **AC 源時失敗**：使用正弦波電壓源時，二極體輸出始終為 0
3. **輸入有變化**：V_ac 正常變化（3.884V, 4.195V, -1.291V...）
4. **輸出固定為 0**：V_out 始終是 0.000V

## 診斷輸出

從診斷日誌中發現：
- 二極體的電容計算正常工作（capValue ≈ 21pF）
- G_coeff 存在且正常（≈ 2e7）
- 電壓和電流在 DC 時正常（Vd ≈ 0.69V, Id ≈ 4.3mA）

但是**沒有看到 AC 整流測試中的 DIODE 診斷輸出**！

## 可能原因

### 假設 1: 節點映射問題 ❓
可能 `n_out` 節點在 AC 電路中沒有正確創建或映射。

### 假設 2: AC 電壓源初始化問題 ❓
AC 電壓源在 t=0 時可能為 0V，導致二極體在整個仿真中保持截止狀態。

### 假設 3: 二極體方向問題 ❓
雖然節點定義看起來正確（anode=n_ac, cathode=n_out），但可能在某個地方極性反了。

### 假設 4: 電流方程問題 ⚠️ **最可能**
二極體的等效電流源 `ceq` 計算可能有誤，導致在瞬態分析中無法正確注入電流。

## 與 MOSFET 對比

MOSFET 的瞬態分析現在**可以工作**（經過修復後）：
- MOSFET DC 測試 ✅
- MOSFET 瞬態測試 ✅
- MOSFET PWM RL 電路 ✅

但 Diode 的瞬態分析**完全失敗**：
- Diode DC 測試 ✅
- Diode 瞬態測試 ❌

## 下一步調試

### 步驟 1: 檢查節點電壓
查看仿真過程中 `n_ac` 和 `n_out` 節點的實際電壓值

### 步驟 2: 檢查二極體的 assemble 調用
確認二極體的 `assemble()` 方法在每個時間步都被正確調用

### 步驟 3: 檢查等效電流源
驗證 `ceq = (cd - gd * vd) + iCapEq` 的計算是否正確

### 步驟 4: 對比 ngspice
查看 ngspice 的 dioload.c 如何處理瞬態分析

## 臨時解決方案

目前 NgDiode 可用於：
- ✅ DC 分析
- ✅ DC 掃描
- ❌ 瞬態分析（待修復）

建議用戶暫時避免在需要瞬態分析的電路中使用 NgDiode，直到此問題解決。

---

**報告日期**: 2025-10-18  
**狀態**: 🔴 待修復
